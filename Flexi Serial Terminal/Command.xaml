<UserControl
	x:Class="Flexi_Serial_Terminal.Command"
	DataContext="{Binding RelativeSource={RelativeSource Self}}"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:local="clr-namespace:Flexi_Serial_Terminal"
	xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
	TextElement.Foreground="{DynamicResource MaterialDesignBody}"
	FontFamily="{materialDesign:MaterialDesignFont}"
	TextElement.FontSize="14px"
	mc:Ignorable="d"
	d:DesignHeight="150" d:DesignWidth="100">
	<materialDesign:Card
		Background="{DynamicResource SecondaryHueLightBrush}"
		Foreground="{DynamicResource SecondaryHueLightForegroundBrush}">
		<StackPanel Width="95">
			<TextBlock TextWrapping="Wrap" Text="{Binding Title}" HorizontalAlignment="Center" FontSize="15" FontWeight="Medium" Margin="5"/>
			<TextBox Text="{Binding CommandName, Mode=TwoWay}" TextWrapping="Wrap" TextAlignment="Center" ToolTip="Command name" Margin="5" MinWidth="90"/>
			<Button materialDesign:ButtonAssist.CornerRadius="13" Content="Send" Margin="5" Click="SendBtn_OnClick" Height="Auto" Width="80" HorizontalAlignment="Center"/>
			<TextBlock TextWrapping="Wrap" Text="{Binding CommandStatus}" TextAlignment="Center" Margin="5" FontSize="12"/>
		</StackPanel>
	</materialDesign:Card>
</UserControl> 